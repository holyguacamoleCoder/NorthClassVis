def run_compact(focus: str | None = None) -> str:
    """Placeholder result; AgentLoop performs the actual history compaction."""
    if focus:
        return f"Compacting conversation (focus: {focus})..."
    return "Compacting conversation..."
